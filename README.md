# Java Quiz App

A full-stack quiz application built with **React (frontend)**, **Node.js + Express (backend)**, and **MongoDB Atlas (database)**.  
Users can select a Java topic/unit, take a timed quiz, and view their scores.

---

## 📂 Project Structure

